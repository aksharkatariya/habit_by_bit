<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HabitByBit Tracker</title>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;600&display=swap" rel="stylesheet">

</head>
    <style>
        /* Basic styles to ensure visibility */
        body {
            font-family: 'Raleway', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100vh; /* Full height for body */
        }

        .header-ribbon {
            background-color: #110d13;
            color: white;
            padding: 15px;
            text-align: center;
        }

        .setup-form {
            flex: 1; /* This allows the form to take up remaining space */
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 20px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            font-weight: 600;
            margin-bottom: 5px;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .submit-btn {
            background-color: #110d13;
            color: white;
            border: none;
            padding: 15px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .bottom-links {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            display: flex;
            justify-content: center;
            padding: 10px;
            background-color: #f8f9fa;
            box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1);
        }

        .bottom-links a {
            text-decoration: none;
            color: #007bff;
            margin: 0 10px;
            font-size: 16px;
        }

        .bottom-links a:hover {
            color: #0056b3;
        }
    </style>
<body>
    <div class="header-ribbon">
      <div class="header-logo">HabitByBit</div>
      <div class="header-log">SETUP</div>
    </div>
      <form class="setup-form" action="/setup" method="post">
      <div class="form-group">
        <label for="habit1">Habit 1: Which habit do you want to track? Write that as a question such that the answer is a number</label>
        <input id="habit1" type="text" name="habit1_param" placeholder="Enter a question">
      </div>
      <div class="form-group">
        <label for="habit2">Habit 2: Which habit do you want to track? Write that as a question such that the answer is a number</label>
        <input id="habit2" type="text" name="habit2_param" placeholder="Enter a question">
      </div>
        <button type="submit" class="submit-btn">Setup my Habits</button>
    </form>
</body>
