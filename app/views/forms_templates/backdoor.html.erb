<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HabitByBit Tracker</title>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;600&display=swap" rel="stylesheet">
    <style>
        /* Basic styles to ensure visibility */
        body {
            font-family: 'Raleway', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            height: 100vh; /* Full height for body */
        }

        .header-ribbon {
            background-color: #110d13;
            color: white;
            padding: 15px;
            text-align: center;
        }

        .setup-form {
            flex: 1; /* This allows the form to take up remaining space */
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 20px;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            font-weight: 600;
            margin-bottom: 5px;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .submit-btn {
            background-color: #110d13;
            color: white;
            border: none;
            padding: 15px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 4px;
            transition: background-color 0.3s;
        }

        .bottom-links {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            display: flex;
            justify-content: center;
            padding: 10px;
            background-color: #f8f9fa;
            box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1);
        }

        .bottom-links a {
            text-decoration: none;
            color: #007bff;
            margin: 0 10px;
            font-size: 16px;
        }

        .bottom-links a:hover {
            color: #0056b3;
        }
    </style>
</head>
    <body>
    <div class="header-ribbon">
      <div class="header-logo">HabitByBit</div>
      <div class="header-log">LOG</div>
    </div>
      <form class="habit-form" action="/add_update" method="post">
      <div class="form-group">
        <label for="task1"><%= current_user.habit_one %></label>
        <input id="task1" type="text" name="task1_param" placeholder="Enter number">
      </div>
      <div class="form-group">
        <label for="task2"><%= current_user.habit_two %></label>
        <input id="task2" type="text" name="task2_param" placeholder="Enter number">
      </div>
      <div class="form-group">
        <label for="motivation_entry">Write a message to motivate yourself tomorrow</label>
        <input id="motivation_entry" type="text" name="motivation_param" placeholder="Share your motivation">
      </div>
        <button type="submit" class="submit-btn">Track My Habits</button>
      </form>
    </body>
</html>
