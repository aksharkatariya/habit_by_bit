<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
    <style>
      body {
        font-family: 'Raleway', sans-serif;
        background-color: #dfbf60;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin: 0;
        padding: 20px;
        box-sizing: border-box;
      }
      
      .header-ribbon {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        background-color: #110d13;
        color: #dfbf60;
        text-align: left;
        padding: 15px 15px;
        font-size: 44px;
        font-weight: 650;
        letter-spacing: 2px;
        display: flex;
        align-items: center;
        z-index: 1000;
        margin-top: 10px;
        margin-top: 6px;
      }
      .charts {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 30px;
        margin-top: 100px;
      }
      .header-logo {
        margin-right: 15px;
      }
      .header-log {
      background-color: #dfbf60;
      color: black;
      padding: 5px 10px;
      font-size: 22px;
      font-weight: 600;
      border-radius: 4px;
      }
      .content {
        margin-top: 100px;
        width: 100%;
        max-width: 1200px;
      }
      h1 {
        color: #110d13;
        font-size: 36px;
        text-align: center;
        margin-bottom: 20px;
      }
      a {
        display: block;
        margin: 20px auto;
        text-align: center;
        color: #110d13;
        font-size: 18px;
        font-weight: bold;
        text-decoration: none;
      }
      a:hover {
        text-decoration: underline;
      }
      ul {
        list-style-type: none;
        padding: 0;
        color: #110d13;
      }
      li {
        font-size: 18px;
        margin: 5px 0;
      }
      .chart-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
      }
      #reading_calendar, #calendar_basic {
        width: 100%;
        max-width: 1000px;
        height: 350px;
        border: 2px solid #110d13;
        border-radius: 8px;
        background-color: white;
      }
.bottom-links {
  position: fixed; /* Sticks the links to the bottom of the viewport */
  bottom: 0;
  left: 0;
  width: 100%; /* Full width of the viewport */
  display: flex;
  justify-content: center; /* Centers the links horizontally */
  align-items: center; /* Aligns the links vertically in the container */
  padding: 10px;
  background-color: #f8f9fa; /* Optional: Background color for the bottom bar */
  box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1); /* Optional: Shadow for a subtle effect */
  z-index: 1000; /* Ensure it's above other elements but doesn't cover the whole page */
}

/* Styling for the links */
.bottom-links a {
  text-decoration: none;
  color: #007bff; /* Link color */
  margin: 0 10px; /* Horizontal space between links */
  font-size: 16px; /* Font size */
}

.bottom-links a:hover {
  color: #0056b3; /* Darker color on hover */
}

/* Ensure the page content doesn't overlap with the fixed bottom links */
body {
  margin-bottom: 50px; /* Add bottom margin to ensure content isn't covered */
}
    </style>
  <body>

    <nav>
<div class="bottom-links">
    <% if current_user != nil %>
    <div>
          <a href="/users/sign_out">Sign out  </a>
    </div>
    <div>
          <a href="/users/edit">Edit Profile  </a>
    </div>
    <div>
          <a href="/setup">Habit Setup  </a>
    </div>
      <% else %>
    <div>
          <a href="/users/sign_up">Sign up  </a>
    </div>
    <div>
          <a href="/users/sign_in">Sign in  </a>
    </div>
    <% end %>
</div>



    </nav>
    <%= yield %>
  </body>
</html>
